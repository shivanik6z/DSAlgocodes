using namespace std;

int main() {
	//code
	int t;
	cin>>t;
	while(t--)
	{
	    int n;
	    cin>>n;
	    int a[n];
	    for(int j=0;j<n;j++)
	    cin>>a[j];
	    
	    int temp=a[n-1];
	    
	    for(int i=n-1;i>0;i--)
	    {
	        a[i]=a[i-1]; 
	    }
	    a[0]=temp;
	    for(int k=0;k<n;k++)
	    cout<<a[k]<<" ";
	    
	    cout<<"\n";
	}
	return 0;
}
